<server description="new server">

    <!-- Enable features -->
<featureManager>
         <feature>webProfile-8.0</feature> 
       <!--  <feature>servlet-4.0</feature> -->
       <!--  <feature>localConnector-1.0</feature>-->
    </featureManager>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint host="*" id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" />

<!-- <webApplication id="veda-itg-core" location="config/dropins/veda-itg-core-0.0.1-SNAPSHOT.war" name="veda-itg-core"/>-->
<!--<webApplication contextRoot="veda-itg-core" location="veda-itg-core-0.0.1-SNAPSHOT.war" />-->

    <!-- Default SSL configuration enables trust for default certificates from the Java runtime --> 
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />

</server>